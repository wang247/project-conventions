---
name: project-conventions
description: "项目编码约定生成器：通过逐领域追问（界面、交互、后端架构、数据与 API 契约、健壮性、安全、测试、工程规范）与用户对齐偏好，最终在仓库根目录生成一份单文件中文 AGENTS.md，作为所有 AI 编码代理的项目约定。This skill should be used when the user wants to establish AI coding conventions for a project, mentions '项目约定'、'编码约定'、'建立项目约定'、'生成 AGENTS.md'、'定约定/规范', or starts a new project that needs consistent AI coding rules. Self-contained and portable: requires only file read/write and conversation."
version: 1.4.0
agent_created: true
---

# Project Conventions — 项目编码约定生成器

## 目的

在项目启动或首次合作时，通过结构化追问与用户对齐 8 大领域的编码偏好，把共识固化为仓库根目录的一份单文件 `AGENTS.md`。此后所有 AI 编码代理（以及人类协作者）写代码前读取该文件即可遵守同一套约定，避免每次都重新口头约定、避免 AI 自由发挥。

本技能是**生成器**而非约定库：它负责"谈判 + 定稿"，约定内容以用户确认的结果为准，本文件中的默认约定仅作为提案底稿。

## 可移植性说明

- 本技能只依赖"文件读写 + 对话"两种能力，不使用任何平台专属工具或绝对路径。
- 支持skills 机制的 agent（WorkBuddy / Claude Code / zcode 等）：将本文件夹整体拷入其技能目录即可。
- 不支持技能机制的 agent（部分 harness）：把本文件正文（不含 frontmatter）整体粘贴进系统提示词或项目指令文件即可，流程完全相同。

## 流程

### 第 0 步：判断运行模式

- **模式 A（新项目）**：仓库为空或几乎没有代码 → 直接进入第 1 步问技术栈。
- **模式 B（现有项目）**：仓库已有代码 → 先扫描代码库（读 `package.json` / `pyproject.toml` / `go.mod` / 目录结构 / 现有配置文件），推断技术栈与当前实际做法，再进入第 2 步。扫描同时收集三张地图（文档/代码/测试）的初版素材。提议约定时**优先匹配现状**，与现状冲突的默认约定要明确标注"这是对现状的变更"并单独确认。若已存在 `AGENTS.md`，先读它，进入**增量更新模式**：只追问缺失或需修订的领域，改完 diff 确认后写回，不得静默覆盖用户的自定义内容。

### 第 1 步：确定技术栈（最多 4 问）

用一次提问收集（模式 B 时改为陈述推断结果请用户确认）：

1. 语言与主框架（如 React / Vue / Next.js / FastAPI / Go 等）
2. UI 方案（如 Tailwind / 组件库 / 原生 CSS）
3. 后端形态与数据存储（单体 / 前后端分离 / BFF；SQL / NoSQL / 无后端）
4. 部署与运行环境（Vercel / 自建服务器 / 本地脚本等）

后续所有默认约定都要按该技术栈**具体化**：写"用 TanStack Query 管理服务端状态"而不是"用一个状态管理库"。同时对照"栈附加包"章节，判断命中哪些附加包，命中的条目并入下一阶段对应领域的清单。

### 第 2 步：逐领域拷问（8 轮）

按以下顺序逐领域进行，前 4 领域（界面、交互、后端与架构、数据与 API 契约）通常对后续决策影响最大，必须走完；后 4 领域（健壮性、安全、测试、工程规范）在用户明确表示"全按默认"时可合并为一次总确认。

**每一轮的固定动作**：

1. 从下方"默认约定库"取出该领域条目（含第 1 步命中的栈附加包条目），按技术栈具体化，编号列出。
2. 明确请用户回复三选一：**全收 / 改第 N 条（说改成什么）/ 补充新条目**。
3. 记录定稿，进入下一领域。不得跳过未确认的领域，不得把未确认条目写入 AGENTS.md。

### 第 3 步：写入 AGENTS.md

将定稿写入仓库根目录 `AGENTS.md`，结构见下方"输出模板"。写入前向用户展示最终文件摘要（各领域条目数），确认后落盘。

### 第 4 步：收尾

用一段话总结关键决定（技术栈、与默认不同的个性化选择、对现状的变更）。提醒用户：

- 在项目后续会话中说一句"先读 AGENTS.md 再动手"即可让约定生效。
- 需求变化时重新运行本技能即可增量更新。

## 默认约定库

以下为提案底稿。应用时逐条按技术栈具体化，与用户确认后定稿。每条必须具体、可检查，禁止"视情况而定"。

### 1. 界面（UI）

- 组件优先：重复出现两次以上的 UI 结构必须抽组件，禁止复制粘贴。
- 样式方案唯一：项目内只用一种样式方案，禁止混用。
- 状态分层：服务端状态与客户端 UI 状态分开管理，服务端数据不塞进全局 store。
- 三态必做：任何列表/详情页必须显式处理 loading、empty、error 三种状态。
- 设计令牌：颜色、间距、字号只用 theme tokens / CSS 变量，禁止硬编码魔法数字。
- 响应式：定义统一的断点档位，新页面必须覆盖移动端。
- 图片显式设置宽高或宽高比占位，防止布局抖动；必须有 alt（装饰图用 `alt=""`）。
- 语义化优先：动作用 `<button>`、导航用 `<a>`/`<Link>`，禁止 `<div onClick>` 假按钮；图标按钮必须 `aria-label`，装饰性图标 `aria-hidden="true"`。
- 焦点必须可见：交互元素提供可见焦点态（`:focus-visible`），禁止裸 `outline: none` 且不提供替代。
- 动画克制：只动 `transform`/`opacity`，禁止 `transition: all`（需逐属性列出），尊重 `prefers-reduced-motion`。
- 长列表（超过 50 项）必须虚拟化或用 `content-visibility` 优化；长文本必须截断/折行（`truncate` / `line-clamp` / `break-words`）。
- 异步更新提示（toast、校验结果）用 `aria-live="polite"` 播报，保证读屏可感知。
- 优先使用平台原生能力（原生 dialog、表单校验等），造轮子前先确认原生方案不够用。

### 2. 交互（Interaction）

- 所有提交类按钮必须有 pending 态并防重复提交。
- 破坏性操作必须二次确认，确认文案需说明具体后果（删什么、影响谁），禁止裸 confirm。
- 异步反馈：成功用轻提示，失败用可关闭的错误提示并给出下一步动作。
- 客户端校验只为即时体验，最终以服务端校验为准；错误必须定位到具体字段，提交失败聚焦第一个错误字段。
- 表单细节：输入用正确的 `type` / `inputmode` / `autocomplete`，禁止禁用粘贴。
- 异步按钮契约：loading 态必须在 `finally` 中重置，成功、失败、异常任何路径都不得卡死。
- 禁止"仅改本地状态不调接口"的假保存/假删除：一切写操作必须真实请求后端。
- 弹窗契约：关闭必须重置内部状态与表单数据，确认必须真实发起请求。
- 前端功能交付前输出交互自检：弹窗（能否关、是否重置）、表单（能否提交、错误展示）、异步按钮（是否请求、loading 是否复位）逐项核对。
- 键盘可达：表单可回车提交，弹窗可用 Esc 关闭。
- 加载优先骨架屏，按钮内小加载用 spinner。
- URL 反映状态：筛选、分页、标签页等视图状态写入 URL query 参数，可深链、刷新后可恢复。
- 路由返回时保留列表页的滚动位置与筛选条件。
- 任何未完成的假实现必须留 `FIXME` 注释说明，禁止伪装成可用功能。

### 3. 后端与架构

- 分层明确：路由层只做参数解析与响应组装，业务逻辑进 service 层，数据访问进 repository/DAL 层。
- 单一职责：单文件超过约 300 行必须拆分或书面说明原因。
- 配置集中：环境变量统一经 config 模块读取并校验，业务代码禁止直接读 process.env / os.environ。
- 错误统一：异常统一走错误处理中间件/统一异常出口，业务代码禁止裸 try-catch 吞错误。
- 日志结构化：关键路径打点（入参摘要、耗时、结果），日志禁止包含密钥、密码、完整身份证/手机号。
- 时间一律存 UTC 或时间戳，只在展示层转本地时区。
- 依赖注入优于全局单例，保证可测试性。

### 4. 数据与 API 契约

- 契约先行：API 先定义契约（OpenAPI / TypeScript 类型 / Zod schema 三选一），实现必须与契约一致。
- 响应结构唯一：统一 envelope（如 `{code, data, message}`）或标准 RESTful + HTTP 状态码，二选一，全项目一致。
- 错误码集中定义在单一文件，禁止散落魔法数字。
- 分页约定全项目统一（page/pageSize 或 cursor），所有列表接口一致。
- 数据库 schema 变更必须走迁移工具生成迁移文件，禁止手工改表。
- 第三方服务必须加适配层隔离 + 超时 + 重试上限，业务代码禁止直接调用第三方 SDK。
- 字段命名全项目统一（camelCase 或 snake_case），接口禁止返回数据库原始结构，一律经 DTO 映射。

### 5. 健壮性（Robustness）

- 边界校验：所有外部输入（用户输入、第三方响应、文件、环境变量）在系统边界处校验后才能进入内部逻辑。
- 所有异步调用必须有错误处理，禁止 floating promise。
- 重试必须带指数退避与次数上限。
- 极端情况显式处理：空数组、null、超大输入、并发冲突。
- 多表/多步写入必须包事务，明确事务边界。
- 写操作接口考虑幂等（幂等键或唯一约束兜底）。
- 非核心依赖失败时核心功能必须可用（降级与兜底）。

### 6. 安全（Security）

- 密钥只存环境变量或密钥管理服务，任何密钥不得出现在前端代码、日志、git 提交中。
- 用户输入在服务端校验与转义；输出按上下文转义防 XSS。
- SQL 一律参数化查询，禁止字符串拼接。
- 鉴权在每个服务端接口强制执行；前端隐藏入口不算权限控制。
- 文件上传校验类型、大小、重命名，存储目录不可执行。
- CORS 白名单最小化，禁止 `*` 与凭证同用。
- 敏感操作（改密、支付、删除）要求二次验证并记录审计日志。
- 公开接口必须有基础速率限制。
- 功能交付前安全自检：逐项核对三类高危——注入（拼接 SQL/命令）、越权（服务端每个接口是否强制鉴权）、敏感信息泄露（日志、前端代码、错误信息中是否暴露密钥/凭证/隐私）。
- 新增依赖前说明理由，优先标准库与已有依赖。

### 7. 测试策略（Testing)

- 核心业务逻辑以单测为主，关键路径少量集成测试，端到端只覆盖 1-3 条最核心链路。
- 修复 bug 前必须先写复现测试。
- 纯函数/工具函数必须有单测；UI 组件不强求快照测试。
- 测试命名描述行为（"应该在余额不足时拒绝转账"），不描述实现。
- mock 只用于外部依赖（网络、时间、文件），禁止 mock 被测对象本身。
- 测试数据用工厂函数生成，fixture 之间不共享可变状态。

### 8. 工程规范（Engineering）

- 目录按功能模块划分（feature-based），不按技术类型堆砌。
- 命名随语言惯例，但项目内必须统一并写入本文件。
- Git 小步提交，提交信息用 Conventional Commits；禁止提交密钥、构建产物、node_modules。
- 格式化交给工具（Prettier / Black / gofmt 等），人工与 AI 不讨论格式。
- 注释只写"为什么"，不写"是什么"；公共 API 写文档注释。
- 禁止过度设计：不为"未来可能的需求"提前写抽象层。

## 栈附加包（按技术栈注入）

附加包是条件条目：**仅当项目技术栈命中时**，在第 2 步开始前把对应包的条目并入相关领域的清单一并确认，未命中则完全不提。目的是让约定库保持精简，同时保留深度规则的注入通道。

### React / Next.js 附加包（命中：技术栈含 React 或 Next.js）→ 并入「界面/交互」领域

- 独立异步操作必须 `Promise.all` 并行，禁止串行 await 造成请求瀑布（含 server 端取数）。
- 禁止在组件内部定义组件；静态 JSX 提取到组件外部。
- 派生状态在渲染期间计算，禁止用 `useEffect` 设置派生状态。
- 重组件用 `next/dynamic` / `React.lazy` 动态导入；第三方分析/日志脚本延后到 hydration 后加载。
- 客户端数据请求统一用 SWR / TanStack Query 自动去重，禁止裸 `useEffect` + fetch。

### 复杂业务后端 DDD 附加包（命中：复杂业务规则多、长期维护的后端服务）→ 并入「后端与架构」领域

- 依赖只向内：domain 层零外部依赖，禁止 import 任何数据库/HTTP/框架库。
- 事务边界在应用层 use case；ORM 实体必须经映射转为领域对象，禁止直接当领域实体使用。
- 贫血模型自检：service 中"读状态 → 判断 → 写回"的逻辑必须下沉为实体行为方法（如 `order.confirm()`）。
- 复杂度阶梯：简单 CRUD 禁止引入 DDD/CQRS/事件溯源，先简单后演进。
- 一个事务只写一个聚合；跨聚合一致性用领域事件（最终一致），需要可靠投递时用 outbox 模式。

## 输出模板

```markdown
# AGENTS.md — {项目名} AI 编码约定

> 生成时间：{日期} · 生成方式：project-conventions 技能
> 技术栈：{一行摘要}
> 任何 AI 代理在写代码前必须先通读本文件并遵守全部约定；修改本文件时保持章节结构。

## 1. 界面
- {定稿条目}

## 2. 交互
...

## 8. 工程规范
...

## 文档地图
- {路由规则，见"三张地图"章节}

## 代码地图
- {路由规则，见"三张地图"章节}

## 测试地图
- {路由规则，见"三张地图"章节}

## 工作流程（新功能/需求变更一律照此执行）
1. 需求拷问：逐分支问透设计树，不留"it depends"；不可逆决策追加 ADR。
2. 规格化：写/更新 docs/specs/{功能名}.md，必须包含可判定的验证标准（测试用例/命令输出/截图对比）。
3. 架构级变更：先出数据模型/API/组件树设计，人工确认后才写码。
4. 微步实现：一次一个组件/函数/路由，每步带验证并小步提交。
5. 评审：代码评审 + 安全自检（注入/越权/泄露）+ 交互自检（弹窗/表单/异步按钮）。
6. 收尾：完成"文档同步"章节的一致性自检；关键路径（auth、数据变更、外部 API）交人工终审。

## 文档同步（需求变更时强制执行）
- 需求变更与新建功能走同一入口：先拷问定稿，再改代码，不接口头直接改。
- SPEC 按功能拆文件（docs/specs/{功能名}.md），变更只更新受影响的功能文件，禁止维护单一巨型 SPEC。
- CONTEXT.md 仅在术语含义变化时更新。
- docs/adr/ 只追加、不改旧：决策被推翻时写新 ADR 并标注 superseded by ADR-NNNN，保留决策历史。
- 任何需求变更收尾前，自检文档一致性（含地图更新，见"三张地图"章节）。未完成自检不得交付。

## 三张地图（路由式，禁止文件清单）

地图只写**规则、入口和指针**，不枚举文件——文件清单让 AI 现场扫描，规则不易过期。三张地图默认内嵌 AGENTS.md；单节超过 30 行时拆到 `docs/maps/{地图名}.md`，AGENTS.md 原位只留一行指针。现有项目由第 0 步扫描生成初版，新项目从最小骨架起步、随代码演进补全。

### 文档地图

- 写/改某功能前 → 必读 `docs/specs/{功能名}.md`（无对应文件则先拷问定稿再建）。
- 涉及跨模块/存储/集成决策 → 读 `docs/design/architecture.md` 与 `docs/adr/`。
- 不确定术语含义 → 读 `CONTEXT.md`，禁止自行揣测。
- 任务是写/改操作手册 → 读 `docs/manual/` 下对应文件。

### 代码地图

- 入口文件与技术栈一句话概述。
- 目录路由规则：新代码放哪（如 `src/modules/{feature}/`）、哪类改动先看哪个模块。
- 关键数据流一条主线（如"请求 → 路由 → service → DB → 响应"）。
- 外部依赖集成点：第三方服务适配层、消息队列、定时任务的位置。

### 测试地图

- 测试位置与命名规则。
- 运行命令三件套：单测 / 全量 + lint / typecheck（AI 最常猜错命令，必须写明）。
- fixture / 测试数据工厂位置。
- 何种改动必须补测试（对应测试策略领域的规则）。

## 附：个性化说明
{与通用最佳实践不同、仅适用于本项目的特殊约定}
```

## 写作规则（生成 AGENTS.md 时执行）

- 每条约定写成对 AI 的祈使句（"生成代码时……"），具体、可检查、可判定违反。
- 禁止出现"视情况而定 / it depends / 尽量"这类不可执行表述；确实有条件分歧时写明条件分支。
- 正文用中文，代码、术语、示例保留英文。
- 不写入未与用户确认的条目；用户删除的默认条目不得残留。
